<h1> New Student</h1>

<%= form_for @student do |f| %>
 <%= f.label :name %>
 <%= f.text_field :name %>
<br>
 <%= f.label :age %>
 <%= f.number_field :age %>
<br>
 <%= f.collection_select :school_id, School.all, :id, :name, prompt: true %>
<br>
 <%= f.submit %>

<% end %>

 
